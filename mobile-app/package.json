{
  "name": "printbuka-staff-app",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor native shell wrapping the Printbuka staff portal (same Laravel app, same central DB — no separate backend).",
  "scripts": {
    "sync": "cap sync",
    "android:open": "cap open android",
    "android:build-debug": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/camera": "^8.0.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/geolocation": "^8.0.0",
    "@capacitor/push-notifications": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
